# Steam File Transfer 기획 및 설계 문서

시작 2026-09-07 · 버전 0.1.1 · 라이선스 MIT

---

## 1. 왜 만드는가

한글패치는 대부분 Windows용 설치 프로그램이다. SteamOS에서는 실행할 수 없다. 그래서 스팀덱에 한글패치를 넣는 절차는 이랬다.

1. 데스크톱 모드로 전환한다
2. 브라우저로 패치 파일을 받는다 (설치형이면 여기서 막힌다)
3. 게임 설치 폴더를 손으로 찾아 들어간다
4. 덮어쓴다
5. 대소문자가 안 맞아 덮어쓰기가 아니라 중복 파일이 생긴 걸 나중에 알게 된다

이 과정을 게임마다, 패치가 갱신될 때마다 반복한다. 패치 파일도 따로 보관해야 한다.

핵심 관찰은 이것이다. **Proton 덕분에 Steam 게임의 파일은 Windows와 SteamOS에서 동일하다.** 실행 파일도, 리소스도 같다. 그러니 패치 파일을 관리할 필요가 없다. 이미 패치가 적용된 기기의 게임 폴더를 다른 기기로 **통째로 덮어쓰면** 된다. Windows PC에서 설치형 패치를 평소처럼 적용하고, 그 게임을 덱으로 보내면 끝이다.

원리만 보면 FTP로 폴더를 복사하는 것과 같다. 이 도구가 더하는 것은 셋이다. 기기와 게임을 자동으로 찾아 고르게 하고, 바뀐 파일만 보내고, 무선이 끊겨도 게임 폴더가 망가지지 않게 한다.

## 2. 핵심 개념

### 게임 단위 동기화

단위는 파일이나 폴더가 아니라 **게임**이다. 사용자는 목록에서 게임을 고를 뿐 경로를 보지 않는다. 앱은 양쪽 기기에서 같은 게임을 찾아, 원본 기기의 파일 전부를 대상 기기에 덮어쓴다. 그중 해시가 같은 파일만 건너뛴다. "이미 있는 파일만" 같은 부분 옵션은 없다. 통째로 맞추는 것이 목적이다.

같은 게임인지는 Steam 앱 ID로 판단한다. 목록에 오르는 게임은 전부 매니페스트가 있어 ID를 안다.

대상 기기에 설치되지 않은 게임은 보낼 수 없다. 파일은 보낼 수 있어도 Steam 클라이언트가 그 게임을 인식하지 못하기 때문이다. 설치된 게임의 파일을 패치된 상태로 맞추는 것까지가 이 도구의 범위다.

### Source와 Target

모든 기기는 앱을 켜는 순간 두 역할을 동시에 한다.

| 역할 | 하는 일 |
| --- | --- |
| Source | 자기 게임 폴더의 파일 목록과 해시를 내주고, 요청받은 파일을 읽어 준다 |
| Target | 세션을 열어 필요한 파일만 알려 주고, 임시 폴더에 받아 두었다가, 한 번에 교체한다 |

"보내기"는 이 기기가 Source, 받는 기기가 Target이다. 화면에는 이 방향 하나만 둔다. 반대 방향은 그 기기 앞에서 보내면 되고, 그렇게 해야 Steam 이 설치한 적 없는 게임의 파일이 이 기기로 흘러드는 일이 생기지 않는다. 코드에서는 `SourceAdapter`와 `TargetAdapter` 인터페이스로 추상화했고, 각각 로컬 구현과 원격(HTTP) 구현이 있다. 계획과 실행 코드는 둘을 구분하지 않는다. 그래서 Source와 Target이 둘 다 원격이면 이 기기가 중계하는 것도 공짜로 된다.

중앙 서버는 없다. 기기끼리 직접 주고받는다. 권한 개념도 없다. 같은 망의 기기는 전부 내 기기라고 본다. 유일한 제한은 자기 자신에게 보내지 못한다는 것뿐이다.

### 설정이 없다

설정 화면이 없다. 설정이 필요한 상황이 생기면 그건 이 도구가 실패한 것이다. 복잡한 설정을 할 바에는 데스크톱 모드에서 직접 덮어쓰는 편이 낫기 때문이다. 한 번 설치하면 SteamOS 쪽에서는 그다음부터 아무 일도 없어야 한다.

그래서 기기 이름은 호스트명, 포트는 고정, Steam 경로는 자동 탐지, 자동 시작은 항상 켜짐이다. 비상용으로 환경 변수(`SFT_STEAM_ROOT`, `SFT_CONFIG_DIR`)만 남겼다. 내부적으로 기기 ID와 이름을 담은 `config.json`이 있지만 사용자가 볼 일은 없다.

### 큐

보내기는 큐에 쌓여 하나씩 처리된다. 사용자는 원하는 게임을 연달아 누르기만 하면 된다. 동시에 여러 게임을 밀면 디스크와 무선이 서로 방해하므로 순차가 맞다. 큐는 메인 프로세스가 갖고, 상태가 바뀔 때마다 화면에 통째로 보낸다.

큐의 기록은 곧 이 앱의 로그다. 별도의 활동 기록 창은 없다. 대신 작업 목록을 `jobs.json`에 저장해서 앱을 다시 켜도 지난 전송이 그대로 보인다. 진행 중이던 작업은 재시작 시 "앱이 종료되어 중단됨"으로 표시된다. **완료된 항목 지우기**가 이 기록을 비우는 유일한 방법이다.

### 백그라운드 서비스와 화면의 분리

수신은 화면이 떠 있지 않아도 되어야 한다. 그래서 프로세스는 백그라운드 서비스로 상주하고(트레이 아이콘), 화면은 필요할 때 여는 클라이언트다. 창을 닫아도 서비스는 살아 있고, 다시 실행하면 이미 떠 있는 프로세스가 창을 연다(단일 인스턴스). Windows와 macOS는 로그인 시 `--daemon`으로 자동 시작한다.

SteamOS는 수신 서버가 별도 데몬(systemd, `ELECTRON_RUN_AS_NODE`)이다. 이때 화면은 포트 충돌을 감지해 데몬의 `/api/status`로 수신 현황을 가져온다. 화면이 떠 있으면 어느 모드든 지금 들어오는 전송이 보인다.

## 3. 동작 흐름

### 기기 찾기

```
[Deck]  --- UDP 37020 브로드캐스트/멀티캐스트 ---> [PC]
        <--- 3초마다 서로 광고, 10초 무응답이면 목록에서 제거 ---
```

mDNS나 Bonjour 같은 OS 기능에 의존하지 않는다. 윈도우에 Bonjour가 없거나 SteamOS에서 avahi가 꺼져 있어도 동작해야 한다. 앱이 직접 UDP 소켓을 열고 멀티캐스트 `239.255.42.99`와 각 인터페이스의 브로드캐스트 주소로 함께 쏜다. 새로 켜진 기기는 질의 패킷을 보내고, 받은 기기들은 유니캐스트로 즉시 응답한다. 무선 랜을 나중에 연결하는 경우를 대비해 3초마다 새 인터페이스를 확인한다.

### 게임 목록

Steam의 `libraryfolders.vdf`로 라이브러리 루트를 전부 찾고, 각 루트의 `appmanifest_*.acf`로 이름과 앱 ID, 설치 폴더를 얻는다. **매니페스트가 있고 설치 폴더도 있는 게임만 목록에 넣는다.** `steamapps/common`에 폴더만 남은 것은 뺀다. Steam은 게임을 지울 때 자기가 설치한 파일만 지우므로 게임이 만든 로그나 설정이 든 빈 껍데기 폴더가 흔히 남는데, 그건 Steam이 설치된 것으로 알지 못하는 폴더라 동기화해도 의미가 없다. 같은 게임인지는 앱 ID로만 판단한다.

아이콘은 Steam이 캐시해 둔 `appcache/librarycache/<appid>/` 안의 32픽셀 이미지를 쓴다. 파일 이름이 해시라 확장자와 크기로 고른다. 원격 기기의 아이콘은 HTTP로 받아 온다.

### 전송: 세션 → 임시 폴더 → 교체 → 검증

무선 기기는 중간에 끊긴다. 파일을 게임 폴더에 곧바로 쓰면 끊긴 순간 그 게임은 반쯤 패치된 상태가 된다. 그래서 4단계로 나눴다.

```
1. 세션 열기   Source 가 파일 목록(경로, 크기, 해시)을 Target 에 보낸다.
               Target 은 각 파일을 자기 게임 폴더와 비교해 "받아야 할 것" 만 추려 돌려준다.
               이미 같은 파일, 임시 폴더에 받아 둔 파일은 빠진다.

2. 받기        필요한 파일만 Target 의 임시 폴더로 보낸다.
               <라이브러리>/steamapps/.sft-staging/<게임>/<세션>/ 에 쌓인다.
               게임 폴더는 아직 건드리지 않는다.

3. 교체        전부 도착했을 때만 한다. 파일마다 rename 으로 자리를 바꾼다.
               하나라도 못 받았으면 교체하지 않고 임시 폴더를 남긴다.

4. 검증        전체 파일이 Source 와 같은지 다시 비교해 알려 준다.
               참고 정보이지 성공/실패의 기준은 아니다.
```

세션 ID는 파일 목록의 내용으로 정한다. 그래서 끊겼다가 같은 원본으로 다시 시도하면 자연히 같은 세션이 되어 이어받고, 원본이 바뀌었으면 새 세션이 된다. 클라이언트가 따로 상태를 기억할 필요가 없다.

임시 폴더는 게임 폴더가 아니라 `steamapps` 바로 아래에 둔다. 게임 폴더와 같은 볼륨이라 rename이 원자적이고, 게임 폴더 안을 보는 도구들 눈에 띄지 않으며, Steam도 무시한다. 파일 목록을 만들 때는 이 폴더를 항상 뺀다. 7일 넘게 남은 세션은 다음 전송 때 정리한다.

파일은 스트림으로 흘리면서 해시를 함께 계산한다. 메모리에 올리지 않으므로 수 GB짜리 파일도 문제없다. 해시가 어긋나면 임시 파일을 지우고 실패로 보고한다.

### 되돌리기는 없다

덮어쓰기 전 원본을 백업하고 되돌리는 기능은 넣지 않았다. Steam의 게임 파일 무결성 확인이 그 역할을 이미 한다. 이 도구가 지켜야 할 것은 "전송 도중 끊겨도 게임 폴더가 반쯤 바뀐 상태로 남지 않는 것"이고, 그건 임시 폴더와 일괄 교체로 해결된다. 백업 기록을 따로 관리하면 그 기록 자체가 깨질 수 있는 새로운 취약점이 된다.

## 4. 까다로운 부분들

### 대소문자

한글패치는 대부분 Windows에서 만들어져 파일명 대소문자가 원본과 다르다. SteamOS는 리눅스라 `Data/Text.loc`과 `data/text.loc`이 다른 파일이다. 그대로 복사하면 원본 옆에 아무도 읽지 않는 파일이 하나 더 생기고 게임은 영어 그대로다.

그래서 쓰기 전에 경로의 각 조각을 실제 디렉터리 목록과 대조해 철자만 다른 기존 항목을 찾는다. 중간 폴더도 마찬가지다. 그다음 `realpath`로 한 번 더 정규화하면 심볼릭 링크와 Windows 8.3 단축 이름(`VERYLO~1.PAK`)까지 정리된다.

### 쓰기 전에 거부하는 것들

남의 게임 파일을 지우고 새로 쓰는 코드라 다음은 아예 쓰지 않고 거부한다. 세션에 파일을 받기 전 단계에서 걸러 낸다.

**같은 이름의 폴더가 있을 때.** 수신 기기에 `data/` 폴더가 있는데 원본에 `data`라는 파일이 있으면, 순진하게 짜면 폴더 트리를 통째로 지우고 파일을 놓는다. 세이브 데이터가 영구 소실되는데 화면에는 성공으로 뜬다. 첫 검토에서 실제로 재현된 시나리오다.

**게임 폴더 밖을 가리키는 심볼릭 링크.** 경로 문자열은 `..`을 막아도 링크는 막지 못한다. Proton prefix의 `drive_c/users/steamuser/Documents`는 실제 홈 디렉터리로 향하는 링크다. 인증이 없으므로 같은 망의 누구나 이 경로로 홈에 파일을 쓸 수 있게 된다. 정규화한 실제 경로가 게임 폴더 안인지 확인한다. 안쪽을 가리키는 링크는 링크가 가리키는 파일에 써서 링크 구조를 유지한다.

**끊어진 링크, 경로 중간의 파일.** 어디에 쓰일지 알 수 없거나 폴더를 만들 수 없는 경우다. 원시 `EEXIST` 오류 대신 무엇이 문제인지 설명한다.

### 게임 실행 중 전송

게임이 실행 중이면 일부 파일이 잠겨 교체가 실패한다. 실패한 파일은 임시 폴더에 남고 나머지는 교체된다. 마지막 검증에서 "다름"으로 표시되므로 사용자는 게임을 끄고 다시 보내면 되고, 이어받기라 금방 끝난다.

## 5. 화면

화면은 세 패널이다. 왼쪽 위 **게임 목록**(이 기기), 오른쪽 위 **기기 목록**(맨 위가 이 기기, 아래로 다른 기기), 아래 **전송 상태**. 패널 사이 홈이 크기 조절 손잡이다. 평소에는 보이지 않다가 잡고 끄는 동안만 색이 들어와 "지금 영역을 바꾸는 중"임을 알린다. 비율로 기억해서 창 크기가 바뀌어도 유지되고, 더블클릭하면 기본으로 돌아온다.

행 단위 동작은 전부 단색 아이콘 버튼이고 이름은 툴팁으로 보여 준다. 게임 목록의 항목에는 폴더 열기뿐이다. 전송은 기기 목록에서 한다.

기기 목록은 접었다 펴는 트리가 아니라 2단계 화면이다. 1단계는 기기 행이고, 다른 기기를 누르면 2단계인 그 기기의 게임 목록으로 들어가며 뒤로 가기로 나온다. 이 기기는 1단계에 표시만 되고 들어갈 수 없다. 자기 자신에게 보낼 일이 없기 때문이다. 2단계의 게임 항목에는 보내기(이 기기의 파일을 그 기기에) 하나만 있다. 아이콘은 내려받기 아이콘과 대칭인 올려보내기 화살표다. 어느 기기로 보낼지 고르는 대화상자는 이 구조 덕에 필요 없다. 패널 머리에는 새로고침과 완료 항목 지우기가 있고, 개수 표시는 어디에도 없다. 체크박스와 단계 번호, 상태 표시등, 활동 기록 창도 없다. 전송 상태 패널이 곧 상태다.

전송 상태의 항목마다 일시정지, 재개, 취소가 있고 끝난 항목에는 제거가 있다. 일시정지는 진행 중인 파일을 버리고 파일 경계에서 멈춘 뒤 큐에 대기(일시정지)로 남는다. 그동안 다음 작업이 진행되고, 재개하면 다시 순서에 들어 새로 계획하므로 받아 둔 파일은 건너뛴다. 교체와 검증 중에는 멈추지 않는다. 짧고, 중간에 끊으면 오히려 손해라서다.

알림은 OS 대화상자 대신 화면 안의 딤 처리된 자체 상자로 띄운다.

타이틀바는 창 안으로 통합했다. `titleBarStyle: 'hidden'`에 Windows/Linux는 창 제어 버튼을 겹쳐 그리고, macOS는 신호등만 남긴다. 창 본문과 같은 색이고 경계선이나 그라데이션이 없으며, 아이콘과 제목만 세로 가운데에 놓인다. 상단 바 전체가 드래그 영역이다. 플랫폼별 여백은 `body`의 클래스로 준다.

## 6. 백그라운드 서비스와 창

한 프로세스가 서비스와 화면을 겸한다. Electron의 단일 인스턴스 잠금으로 두 번째 실행은 첫 프로세스에 "창을 열라"는 신호만 보내고 끝난다. 창의 닫기 버튼은 창을 숨길 뿐이고, 종료는 트레이 메뉴에서 한다. 앱 종료 이벤트에서만 진짜로 닫히도록 플래그를 둔다.

SteamOS의 데몬은 Electron을 `ELECTRON_RUN_AS_NODE=1`로 실행한다. 크로미움을 띄우지 않고 Node 런타임만 쓰므로 화면이나 그래픽 드라이버 없이 게임 모드에서도 상주한다. `Nice=10`, `IOSchedulingClass=idle`로 게임 실행 중에는 뒤로 물러난다.

## 7. 기술 선택

### 왜 Electron인가

| 후보 | 장점 | 문제 |
| --- | --- | --- |
| **Electron** | 한 환경에서 세 OS용을 다 뽑음. UI를 웹으로 만들어 빠름. 서비스와 화면이 같은 코드 | 용량 약 100MB |
| .NET + Avalonia | 실행 파일 하나, 용량 작음 | GUI 세부 조정이 번거로움 |
| Flutter | UI 품질 좋음 | 리눅스 빌드를 리눅스에서 해야 함 |
| Tauri (Rust) | 가볍고 빠름 | 리눅스 빌드에 시스템 라이브러리 의존 |

단일 빌드 환경과 일관된 GUI를 우선하면 Electron이 맞다. 병목은 네트워크와 디스크에 있지 UI에 있지 않다. 용량은 수 GB짜리 게임을 다루는 맥락에서 문제가 안 된다. 모두 오픈소스이고 Electron은 MIT다.

### 의존성이 없다

런타임 의존성은 0개다. HTTP 서버와 클라이언트, UDP 탐색, VDF 파서, 해시, 스트리밍을 전부 Node 내장 모듈로 만들었다. 오픈소스로 공개할 때 의존성이 적을수록 감사하기 쉽고 공급망 위험이 줄고 몇 년 뒤에도 그냥 빌드된다. UI도 프레임워크 없이 순수 DOM이다. 개발 의존성은 electron, electron-builder, esbuild, typescript뿐이다.

## 8. 보안 방침

내부망 전용이다. 인터넷에 노출할 물건이 아니다.

- 인증이 없다. 같은 공유기 아래면 바로 주고받는다. 암호나 권한 화면을 만들지 않는다
- 쓸 수 있는 곳은 Steam 게임 폴더 안뿐이다. 경로 탈출과 게임 폴더 밖 쓰기는 항상 차단한다
- Windows 방화벽 규칙은 사설망 프로필에만 연다
- HTTPS는 쓰지 않는다. 내부망에서 인증서를 관리하는 비용이 얻는 것보다 크다

## 9. 코드 구조

```
src/
  core/            # 플랫폼 독립 로직. 화면과 데몬이 공유
    types.ts       # 공용 타입
    vdf.ts         # Valve KeyValues 파서
    steam.ts       # Steam 경로 탐지, 라이브러리 열거, 게임 목록, 아이콘
    fsutil.ts      # 대소문자 무시 경로 해석, 폴더 펼치기, 경로 안전성
    hash.ts        # 스트리밍 SHA-256, 폴더 전체 해시
    patch.ts       # Target 측: 세션, 임시 폴더, 교체, 검증
    inbound.ts     # 이 기기로 들어오는 수신 현황
    discovery.ts   # UDP 기기 탐색
    server.ts      # HTTP API
    client.ts      # HTTP 클라이언트
    transfer.ts    # Source/Target 어댑터, 계획, 실행
    config.ts      # 기기 ID·이름 (사용자 설정 아님)
    paths.ts       # 설정·전송 기록 디렉터리
    node.ts        # 위를 조립한 "한 대의 기기"
  daemon/index.ts  # 헤드리스 진입점 (systemd)
  main/            # Electron 메인: 창, 트레이, 작업 큐와 기록, IPC
  renderer/        # 화면 (HTML/CSS/TS, 프레임워크 없음)
```

## 10. HTTP API

전부 `/api` 아래, JSON. 파일 전송만 바이너리 스트림이다. `root`는 `game`(게임 폴더) 또는 `prefix`(Proton의 `drive_c`).

| 메서드 | 경로 | 용도 |
| --- | --- | --- |
| GET | `/api/info` | 기기 정보 |
| GET | `/api/status` | 이 기기의 수신 현황 |
| GET | `/api/games` | 게임 목록 (매니페스트 + 폴더) |
| GET | `/api/games/:id` | 게임 하나 |
| GET | `/api/games/:id/icon` | 아이콘 이미지 |
| GET | `/api/games/:id/tree?root` | 파일 목록과 해시 (받기용) |
| GET | `/api/games/:id/file?root&path` | 파일 내려받기 (받기용) |
| POST | `/api/games/:id/check?root` | 파일 목록 비교 (검증용) |
| POST | `/api/games/:id/session?root` | 세션 열기. 받아야 할 파일만 돌려줌 |
| PUT | `/api/games/:id/stage?root&session&path&sha256` | 임시 폴더로 파일 받기 |
| POST | `/api/games/:id/commit?root&session` | 임시 폴더의 파일을 제자리로 교체 |

## 11. 배포

### 더블클릭 설치 (Decky Loader 방식)

덱 사용자에게 압축을 풀고 터미널에서 명령을 치라고 할 수는 없다. 릴리스에서 `.desktop` 파일 하나를 받아 데스크톱 모드에서 더블클릭한다.

```
steam-file-transfer-installer.desktop  (사용자가 더블클릭)
  └─ Exec: curl … /releases/latest/download/steam-file-transfer-installer.sh | bash
       └─ get.sh: 최신 tar.gz 를 고정 URL 로 내려받아 풀고
            └─ install.sh: 홈에 복사, 실행 권한 복구, systemd 서비스 등록, 아이콘 등록
```

`Terminal=true`라 konsole 창이 열리고 진행 상황이 보인다. 같은 파일을 다시 더블클릭하면 갱신된다. 부트스트랩은 GitHub API를 쓰지 않는다. 워크플로가 tar.gz를 버전 없는 이름으로 한 벌 더 올리므로 고정 URL로 받는다.

세 파일이 같은 이름을 공유한다. `.desktop`의 URL, `get.sh`의 REPO와 ASSET, 워크플로가 올리는 자산 이름이다. 하나만 바꾸면 조용히 깨지므로 `scripts/check-deploy.mjs`가 셋의 일치를 검사한다. 리포지터리 이름은 `ddukbaek2/steam-file-transfer`를 기본값으로 두었다.

### 릴리스 파이프라인

`.github/workflows/release.yml`이 `v*` 태그를 밀면 ubuntu, windows, macos 러너에서 각각 테스트하고 빌드한 뒤 릴리스 하나에 모아 올린다.

### Windows에서 리눅스용을 만들 때의 함정

셋 다 실제로 부딪히고 처리했다. **줄바꿈이 CRLF가 된다.** 리눅스 bash는 `\r`을 명령으로 읽어 첫 줄부터 죽는다. `.gitattributes`에 `*.sh text eol=lf`를 못 박고 점검 스크립트가 CR을 검사한다. **실행 권한이 사라진다.** Windows에서 만든 tar.gz는 모든 파일이 `-rw-r--r--`다. 설치 스크립트가 앱을 찾을 때 실행 권한이 아니라 파일 존재로 판단하고, 복사 후 권한을 다시 부여한다. **AppImage를 만들 수 없다.** `mksquashfs`가 없어서다. tar.gz만 기본 대상이다.

## 12. 테스트

```bash
npm test               # 단위 + 네트워크 + 기동 + 안전장치
npm run e2e            # 빌드된 데몬 두 개를 별도 프로세스로 띄워 실제 HTTP 로 동기화
npm run smoke          # GUI 를 실제로 띄워 렌더링과 IPC 확인, 스크린샷 저장
npm run check:deploy   # 배포 파일 사전 점검
```

목이 아니라 실제 파일시스템과 실제 소켓을 쓴다. 다루는 것은 이렇다.

- VDF 파서, Steam 라이브러리 열거, 매니페스트 없는 잔여 폴더 제외
- 경로 안전성: 상위 탈출, 중간 폴더 철자 복원, 폴더 밖 링크 거부, 폴더를 파일로 덮어쓰기 거부, 실패 시 임시 파일 미잔류
- 세션: 임시 폴더에 받기, 전부 도착했을 때만 교체, 중단 후 이어받기
- HTTP: 세션부터 교체까지, 받기(tree/file), 수신 현황
- 탐색: 실제 UDP 소켓 두 개가 서로를 찾는지
- 기동 모드: 포트가 비었을 때, 우리 데몬이 잡고 있을 때, 남의 프로그램이 잡고 있을 때
- 화면: 실제 창을 띄워 패널·아이콘 버튼·검색 지우기·기기 목록 1단계·크기 조절 손잡이·작업 제거를 확인

테스트는 설정 폴더를 임시 위치로 강제한다. 그렇지 않으면 사용자의 실제 전송 기록 사이에 테스트 찌꺼기가 섞인다. 실제로 한 번 그런 일이 있었다.

독립 코드 검토를 두 차례 받았다. 첫 검토에서 데이터 손실로 이어지는 문제 둘과 심각한 문제 넷을 찾았고, 두 번째 검토로 수정이 실제로 동작하는지 확인받고 셋을 더 고쳤다. 재현된 시나리오는 전부 테스트로 남겼다.

## 13. 환경 변수

| 변수 | 용도 |
| --- | --- |
| `SFT_STEAM_ROOT` | Steam 루트를 직접 지정. 자동 탐지보다 우선 |
| `SFT_CONFIG_DIR` | 기기 정보와 전송 기록 위치 변경. 이식형 실행이나 테스트용 |
| `SFT_INSTALL_URL` | 부트스트랩이 tar.gz 를 받을 주소. 포크나 미러, 로컬 테스트용 |
| `SFT_HTTP_PORT`, `SFT_DISCOVERY_PORT` | 설치 스크립트가 개방할 포트 (기본 37021, 37020) |

## 14. 앞으로 할 것

- 전송 중 파일 여러 개 동시 전송 (지금은 순차. 작은 파일이 많으면 느리다)
- Proton prefix 레지스트리 수정 (폰트 등록 등 파일 복사만으로 안 되는 패치)
- 압축 전송 (텍스트 위주 게임에서 효과)
- CLI 모드
